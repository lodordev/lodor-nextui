desc pasted https://host/ is stripped to a bare host before --set-server
pick 0|Set up now
pick 0|https (secure, recommended)
kb 0|https://romm.example.com/
kb 2|
pick 0|Verify TLS certificate (recommended)
engine 0|RESULT server_set=1
kb 2|
expect_exit 0
expect_trace ENGINE --set-server https://romm.example.com -> rc=0
expect_trace_absent --port
expect_config_has "root_uri": "https://romm.example.com"
expect_config_lacks token
