desc coexist toggle from legacy own (displays as merged-in) switches to separate (issue #68: own always escapable, never re-entered)
config paired
seeded
settings mirror_mode=own
pick 0|RomM games: Mixed into your folders
pick 2|
expect_exit 0
expect_file_has .userdata/shared/Lodor/settings.conf	mirror_mode=separate
expect_trace PRES timeout=3 msg='RomM games get their own folders. Run Refresh library to apply.'
