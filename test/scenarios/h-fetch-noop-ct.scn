# Task #134 guard: NextUI's own pre-launch hook pass fires with the CONTINUE dummy path —
# the (LODORCT) tag guard must no-op instantly (the dispatcher itself runs this hook with
# the REAL path; doing work here would double it against the dummy).
desc fetch hook: no-op on the Continue root entry (tag guard, no engine calls)
invoke prehook
config paired
romarg Roms/0) Continue (LODORCT)/Continue.ct
expect_exit 0
expect_trace_absent ENGINE
