# task #134: a failed reconnect is HONEST — names the real state (didn't reach Running) and
# where the why lives (tailscaled.log); never fake success, never a re-auth prompt.
desc menu: Tailscale: Reconnect failure reports didn't-reach-Running honestly
config paired
seeded
ts present
tsstatus stopped
pick 0|Tailscale: Reconnect
pick 2|
expect_exit 0
expect_trace PRES timeout=-1 msg='Reconnecting Tailscale...'
expect_log err: Couldn't reconnect - Tailscale didn't reach Running. Check Wi-Fi (details: tailscaled.log)
